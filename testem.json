{
  "framework": "mocha",
  "test_page": "spec/index.html",
  "before_tests": "babel src --out-dir .tmp --source-maps; babel spec --out-dir .tmp --source-maps",
  "src_files": [
    "src/**/*.js",
    "spec/index.html",
    "spec/**/*.js",
    "demo/**_demo.js"
  ],
  "serve_files": [
    ".tmp/**/*.js",
    "lib/**/*.js"
  ]
}
